---
const { btntitle, color, xtraclass, link } = Astro.props;
---

<a href={link}>
    <button
        style=`padding: 2px 30px;background-color: ${color};box-shadow: 0px 0px 5px ${color};border: 2px solid ${color};transition: .7s ease-out;`
        class=`rounded-2xl ${xtraclass} font-bold text-2xl mt-1 hover:bg-transparent`
        title={btntitle}
    >
        <slot />
    </button>
</a>